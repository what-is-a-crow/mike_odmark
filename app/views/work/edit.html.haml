= simple_form_for @work, method: "PUT" do |f|
  = f.input :content, as: :text, input_html: { class: "html-editor" }
  = f.submit class: "btn btn-primary"
  = link_to "Cancel", work_path, class: "btn"
